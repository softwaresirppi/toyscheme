(define (apply-twice f)
    (lambda (x)
        (f (f x))))

;; (((apply-twice (apply-twice apply-twice)) incr) 5)

(define (min a b)
    (print MIN-COMPARISON)
    (if (< a b)
        a
        b
    )
)

(define (max a b)
    (print MAX-COMPARISON)
    (if (< a b)
        b
        a
    )
)

(define (minmax numbers)
    (minmax )
)

(open-picture 512 512)
(draw-line 0 0 512 512)
(close-picture)